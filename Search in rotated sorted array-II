class Solution:
    def searchInARotatedSortedArrayII(self, arr, target):
        n = len(arr) 
        
        for i in range(n):
            if arr[i] == target:
                return True
        return False
